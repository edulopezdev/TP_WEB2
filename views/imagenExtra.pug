doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Imágenes Adicionales
    link(rel="stylesheet", href="/css/imagenExtra.css")
  body
    header
      h1 Imágenes Adicionales
      
    // Contenedor para mostrar solo las imágenes adicionales
    if objeto.additionalImages && objeto.additionalImages.length > 0
      .container-imagenes
        each image in objeto.additionalImages
          img(src=image, alt='Imagen adicional', class="imagen-extra")
    else
      p No hay imágenes adicionales disponibles.
